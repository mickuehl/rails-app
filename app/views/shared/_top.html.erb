
<% if account_signed_in? %>
  <%= link_to 'Logout', logout_path, { method: 'delete'} %>
  <p>
  <%= current_account.display_name %>
  <%= current_account.role %>
<% else %>
  <%= link_to 'Register', register_path, data: {turbolinks: false} %>
  <%= link_to 'Login', login_path %>
<% end %>

<p>