
<%= link_to 'Home', root_path %>

<% if account_signed_in? %>
  <%= link_to 'Logout', destroy_account_session_path, { method: 'delete'} %>
  <p>
  <%= current_account.display_name %>
  <%= current_account.role %>
<% else %>
  <%= link_to 'Register', new_account_registration_path, data: {turbolinks: false} %>
  <%= link_to 'Login', account_session_path %>
<% end %>

<p>